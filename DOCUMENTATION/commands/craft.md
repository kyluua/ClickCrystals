[<-- Back to Legend](../legend.md)

# Command Name: Craft
Keyword: craft

### Usages
```
craft <identifier> <num>? <num>?
```

### Regex
```regexp
(((craft)( !?(([#:](\w+)(\[(.*)\])?,?)+))( (-?\d*(\.\d*)?))?( (-?\d*(\.\d*)?))?))
```

### Raw Documentation
```yml
# craft <identifier> <num>? <num>?
```
